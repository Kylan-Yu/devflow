# Shared i18n Skeleton

- EN: Put reusable locale namespaces here when web/admin share the same keys.
- 中文：当 web/admin 共享同一套文案键时，可在此维护通用语言资源。

Current app-level i18n files remain inside each app for independent startup.
当前各应用仍保留独立 i18n 文件，便于单独启动。
